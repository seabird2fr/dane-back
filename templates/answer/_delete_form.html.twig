{% if is_granted('ROLE_TEACHER') %}
    <form method="post" action="{{ path('answer_delete', {'id': answer.id}) }}" onsubmit="return confirm('{{'Etes-vous sur de vouloir supprimer la réponse ?'}}');">
        <input type="hidden" name="_method" value="DELETE">
        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ answer.id) }}">
        <button class="btn btn-danger btn-sm mr-2"><i class="fas fa-trash-alt"></i> {{Effacer}}</button>
    </form>
{% endif %}
